const today=new Date();document.getElementById('today').textContent=today.toLocaleDateString('ko-KR',{year:'numeric',month:'long',day:'numeric',weekday:'short'});const goals=[...document.querySelectorAll('[id^="goal-"]')];const bar=document.getElementById('progressBar');const text=document.getElementById('progressText');function updateProgress(){const done=goals.filter(x=>x.checked).length;const p=Math.round(done/goals.length*100);bar.style.width=p+'%';text.textContent=p+'%'}goals.forEach(x=>x.addEventListener('change',updateProgress));document.getElementById('resetTasks').addEventListener('click',()=>document.querySelectorAll('[id^="task-"]').forEach(x=>x.checked=false));document.querySelectorAll('[data-target]').forEach(b=>b.addEventListener('click',()=>document.getElementById(b.dataset.target)?.scrollIntoView({behavior:'smooth'})));
